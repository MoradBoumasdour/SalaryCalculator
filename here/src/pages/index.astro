---
import BaseLayout from '../layouts/Layout.astro';
import SalaryCalculator from '../components/Calculator';
export const Layout = BaseLayout;
---

<header class="flex justify-end items-center max-w-3xl mx-auto p-4">
  <button
    id="theme-toggle"
    aria-label="Toggle dark mode"
    class="px-3 py-2 border border-gray-300 dark:border-gray-700 rounded-md text-xl"
    aria-pressed="false"
  >
    🌙
  </button>
</header>

<main class="max-w-3xl mx-auto my-8 p-4">
  <SalaryCalculator client:load />
  <hr class="my-8 border-t border-gray-200 dark:border-gray-700" />
</main>

<script>
  
  function toggleTheme() {
    const html = document.documentElement;
    const isDark = html.classList.toggle("dark");
    localStorage.setItem("theme", isDark ? "dark" : "light");
    return isDark;
  }


  (function initTheme() {
    const saved = localStorage.getItem("theme");
    const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;

    const theme = saved || (prefersDark ? "dark" : "light");

    if (theme === "dark") {
      document.documentElement.classList.add("dark");
    }
  })();

  const btn = document.getElementById("theme-toggle");

  if (btn) {
    const isDark = document.documentElement.classList.contains("dark");
    btn.setAttribute("aria-pressed", isDark.toString());
    btn.textContent = isDark ? "☀️" : "🌙";

    btn.addEventListener("click", () => {
      const nowDark = toggleTheme();
      btn.setAttribute("aria-pressed", nowDark.toString());
      btn.textContent = nowDark ? "☀️" : "🌙";
    });
  }
</script>
