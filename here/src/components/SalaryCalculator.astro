---
import Calculator from '../components/Calculator.tsx';
---

<section class="max-w-3xl mx-auto my-8 p-4">
  <header class="flex justify-between items-center mb-6">
    <h1 class="text-2xl font-semibold">Calcolatore RAL &rarr Netto</h1>

    <button
      id="theme-toggle"
      aria-label="Toggle dark mode"
      class="inline-flex items-center gap-2 px-3 py-2 rounded-md border border-gray-200 dark:border-gray-700 bg-white dark:bg-slate-800 text-gray-900 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-indigo-400"
      aria-pressed="false"
    >
      <span id="theme-toggle-icon">🌙</span>
      <span class="sr-only">Toggle theme</span>
    </button>
  </header>

  <p class="text-gray-600 dark:text-gray-300 mb-4">
    Inserisci la RAL (Retribuzione Annua Lorda) e clicca "Calcola".
  </p>

  <!-- isola dinamica di react-->
  <Calculator client:only="react" />

  <hr class="my-8 border-t border-gray-200 dark:border-gray-700" />
</section>